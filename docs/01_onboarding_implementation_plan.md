# AIPO 프로젝트 초기화 및 온보딩 화면 구현 계획

사용자께서 제공해주신 피그마 디자인을 바탕으로, AIPO 프로젝트의 초기화 및 첫 번째 화면인 온보딩(Onboarding) 화면을 개발하기 위한 계획입니다.

## User Review Required

> [!IMPORTANT]
> - 초기 프로젝트를 현재 폴더(`c:\Users\baeki\AIPO`)에 `flutter create .` 명령어를 사용하여 생성할 예정입니다.
> - 이미지에 나타난 기본 색상(Primary Blue)과 폰트 스타일을 기본 Flutter 위젯과 테마로 구현합니다.
> - 프로젝트 초기화를 위해 명령어를 실행하고 구조를 잡는 과정이 포함되어 있으니 아래 계획을 확인해 주시고 승인해 주시면 바로 작업을 시작하겠습니다.

## Proposed Changes

### 1. Flutter 프로젝트 생성
- `c:\Users\baeki\AIPO` 디렉토리에 빈 Flutter 프로젝트를 생성합니다.
- 안드로이드/iOS 에뮬레이터에서 실행할 수 있도록 기본 환경을 구성합니다.

### 2. 폴더 구조 및 파일 정리
기본적인 프론트엔드 설계를 위해 다음과 같은 폴더/파일을 생성/수정합니다.

#### [MODIFY] `lib/main.dart`
- 기본 생성되는 카운터 앱 코드를 모두 지우고, 앱의 시작점(Entry Point) 및 `MaterialApp` 설정을 세팅합니다.
- `home` 속성으로 `OnboardingScreen`을 지정합니다.
- 전역적인 앱 테마(Primary Color, Font Theme)를 간략히 설정합니다.

#### [NEW] `lib/screens/onboarding_screen.dart`
온보딩 화면 UI를 구현하는 메인 파일입니다.
제공해주신 디자인을 다음과 같은 컴포넌트로 구성합니다:
1. **타이틀 영역**: "어려운 공모주 분석, 이제 챗봇과 대화하세요" 텍스트와 서브 텍스트.
2. **챗봇 대화 영역 (Mock UI)**:
   - 사용자의 질문 말풍선 (우측, 연회색 배경)
   - 챗봇의 답변 말풍선 (좌측, 파란색 배경, 흰색 텍스트)
   - 챗봇의 종목 추천 카드 (좌측, 그림자 효과, 종목명, AI 매력지수 뱃지, 청약일/주관사 정보 텍스트)
3. **하단 액션 영역**:
   - 하단에 고정된 **"AIPO 시작하기"** 메인 버튼 (파란색, 라운드 처리)
   - **"이미 계정이 있으신가요? 로그인"** 텍스트 버튼

## Open Questions

> [!NOTE]
> 1. 제공되는 앱의 기본 폰트로 특정 한글 폰트(예: Noto Sans KR, Pretendard)를 바로 적용할까요? 아니면 현재는 시스템 기본 한글 폰트를 사용하고 나중에 구체화할까요?
> 2. `AIPO 시작하기` 버튼과 `로그인` 버튼을 눌렀을 때, 일단은 빈 화면(`LoginScreen`, `MainScreen` 등)으로 이동하도록 껍데기(Dummy) 라우팅을 연결해둘까요?

## Verification Plan

### Automated Tests
- `flutter analyze`를 통해 문법 오류 및 린트(Lint) 에러가 없는지 검증합니다.

### Manual Verification
- `flutter run` 명령어를 통해 제공된 피그마 디자인과 실제 화면이 일치하는지 에뮬레이터(또는 윈도우 데스크톱)에서 렌더링을 확인합니다.
- 각 UI 요소(말풍선, 버튼, 텍스트)의 간격과 색상이 적절한지 검수합니다. 
